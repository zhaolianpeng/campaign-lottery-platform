# 生产上线重构计划

## Phase 0 — 安全与上线阻断项 ✅ 已完成 (2026-05-29)

- [x] 生产环境拒绝 `CORS_ALLOW_ORIGIN=*`
- [x] 部署脚本/PM2 移除硬编码密码，改为环境变量
- [x] `.env.example` 微信占位符与 `PAYMENT_ENABLED` 注释一致
- [x] 支付下单服务端定价目录
- [x] 用户 fulfill 只读；履约走 notify/internal
- [x] 500 响应泛化 + `error_id`
- [x] 奖品图禁止 SVG + magic-byte 校验
- [x] 前端 token 键、`api.ts` 健壮性、生产隐藏 `dev_code`
- [x] `healthz` 增强 + `release_82.sh` 健康检查

## Phase 1+

待后续迭代。
